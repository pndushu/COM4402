BEGIN

  Set score to 0

  Create a LIST called questions_list
  Each item contains:
      - a question
      - a LIST of options
      - the correct answer

  WHILE there are more questions to enter
      Enter next question into questions_list
  END WHILE

  FOR each question in questions_list DO

      Display message (show question)
      Display message (show options)

      Get user's answer
      Store as user_answer

      IF user_answer matches the correct answer THEN
          Increase score by 1
          Display message (show updated score)
      ELSE
          Display message (inform user the answer is incorrect)
      END IF

  END FOR

  Display message (show final score)

END